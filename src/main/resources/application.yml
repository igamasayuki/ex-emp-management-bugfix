# yaml-language-server: $schema=../../../.vscode/schemas/any.json
spring:
  messages:
    basename: messages
    encoding: UTF-8
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: taku
    password: postgres
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
